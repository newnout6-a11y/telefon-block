"""App store crawler modules for the App Category ML Classifier dataset.

Each crawler emits a CSV with columns ``packageName,label,category`` into
``datasets/categories/raw/``. See spec
``app-category-ml-classifier/design.md`` (Component 6) and Requirement 1.1
for the contract.
"""
